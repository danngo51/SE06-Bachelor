import React from 'react';
import FullMap from '../../components/FullMap/FullMap';

const Subdivide = () => {
    return (
        <>
            <FullMap />
        </>
    );
};

export default Subdivide;
